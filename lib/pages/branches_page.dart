import 'package:flutter/material.dart';

class BranchesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Branches')),
      body: Center(child: Text('Branches Page')),
    );
  }
}
